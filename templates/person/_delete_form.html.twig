<form method="post" action="{{ path('persons_delete', {id: person.id}) }}" onsubmit="return confirm('Wirklich löschen?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete_person_' ~ person.id) }}">
    <button type="submit">Löschen</button>
</form>
